---
title: Simplify and expand
topic: A4
domain: algebra
ao:
  - AO1
  - AO2
  - AO3
printNotes: Name · Date · Show each step of your working
---

## Getting started

1. Simplify $x + x$.
2. Simplify $a + a$.
3. Simplify $b + b + b$.
4. Simplify $2c + c$.
5. Expand $2(x + 1)$.
6. Expand $3(y + 1)$.
7. Find $2(a + 2)$ when $a = 1$.
8. Find $3(b + 1)$ when $b = 2$.

## Fluency (AO1)

9. Simplify $3x + 2x$.
10. Simplify $5y - 2y$.
11. Simplify $4x + 3 + 2x$.
12. Simplify $6a - 2a + a$.
13. Simplify $3x + 2y + x + y$.
14. Simplify $5a + 3b - 2a - b$.
15. Expand $2(x + 3)$.
16. Expand $3(x - 4)$.
17. Expand $5(2x + 1)$.
18. Expand $4(3x - 2)$.
19. Simplify $x \times x$.
20. Simplify $3x \times 2$.


## More fluency

21. Simplify $3a + 2a$.

22. Expand $2(x + 4)$.

## Reasoning (AO2)

23. Sam simplifies $3x + 2x$ and gets $5x^2$. Explain Sam's mistake and give the correct answer.

24. **Always, sometimes, or never?** $2(x + 5)$ gives the same result as $2x + 5$.

25. Priya expands $4(x - 3)$ and writes $4x - 3$. Explain what Priya forgot to do, and give the correct expansion.

26. Two expressions, $3(x + 2)$ and $3x + 6$, are claimed to be equivalent. Test this by substituting $x = 4$ into both.

27. **True or false?** You can only collect terms with $x$ in them; terms with $y$ must stay separate. Use $3x + 2y + x$ to support your answer.

28. Leo simplifies $5x - 3x + 2x$ and gets $0x$. Explain Leo's mistake and find the correct simplified expression.

29. Compare simplifying $4x + 3x$ with expanding $4(x + 3)$. Why do these give different types of answers?

30. Mo says $2(x + y)$ expands to $2x + y$. Explain Mo's error and expand it correctly.

## Problem (AO3)

31. A rectangle has width $x$ and length $x + 3$. Write and simplify an expression for its perimeter.

32. Archer buys $x$ pens at 20p each and $x$ pencils at 15p each. Write and simplify an expression for the total cost in pence.

33. A garden has two rectangular beds. One has area $3(x + 2)$ and the other has area $2x$. Expand and simplify an expression for the total area.

34. A shop sells $t$-shirts for £($x + 5$) and shorts for £$x$. Sloan buys 2 t-shirts and 3 pairs of shorts. Write and simplify an expression for the total cost.

35. A triangle has sides $2x$, $x + 3$, and $x + 3$. Write and simplify an expression for its perimeter.

36. A pack of $n$ stickers costs $5(n + 1)$ pence including a fixed handling charge. Expand this expression and explain what the numbers mean.

## Stretch

37. Simplify $3(x + 2) + 2(x - 1)$.

38. Simplify $5(2x - 1) - 3(x + 2)$.

39. Show that $4(x + 3) - 2x$ simplifies to $2x + 12$. Show each step.

40. A rectangle's perimeter is $2(x + 4) + 2(x + 4)$. Simplify this fully, then explain what type of shape this perimeter expression suggests.


## Mixed practice

41. Simplify $5x + 3 - 2x + 1$.

42. Expand and simplify $3(2x - 1) + x$.

## Fluency boost

43. Simplify $2x + 1x$.

44. Simplify $3x + 2x$.

## Fluency boost

45. Simplify $6x + 2x$.

46. Expand $4(x + 3)$.

47. Simplify $9y - 4y + 1$.

48. Expand $2(3x - 1)$.

49. Simplify $5a + 3b - 2a + b$.

50. Expand $-2(x + 5)$.

51. Simplify $x + 4x - 2x$.

52. Expand $5(2x + 1)$.

53. Simplify $7 - 3x + 2 + x$.

54. Expand $3(x + y)$.

55. Simplify $4x^2 + x^2$.

56. Expand $2(x - 4) + 3$.

57. Simplify $10x - 5x + 2 - 7$.

58. Expand $4(2x - 3)$.

59. Simplify $3(x + 1) - 2x$ (expand first).

60. Expand $-3(2 - x)$.

61. Simplify $2x + 3y - x + 5y$.

62. Expand $6(\dfrac{x}{2} + 1)$.

63. Simplify $5(2x) - 3x$.

64. Expand $(x + 2)(x + 3)$.

## Collecting like terms (AO1)

65. Simplify $8x + 5 - 3x + 2$.

66. Expand $7(x - 2) - 4x$.

67. Simplify $4a + 3b - a - 2b$.

68. Expand $2(3x + 4) + 5(x - 1)$.

69. Simplify $x^2 + 3x + 2x^2 - x$.

70. Expand $3(2x - 5) - 2(x + 1)$.

71. Simplify $12y - 3y + 5 - 2y$.

72. Expand $5( x + 2) + 2( x - 3)$.

73. Simplify $6x - 2( x + 1)$ (expand brackets).

74. Expand $(2x + 1)(x + 4)$.

75. Simplify $3x + 4 - 2(x - 1)$.

76. Expand $4(3x - 2) - 3(2x + 1)$.

77. Simplify $5x^2 - 2x^2 + 3x - x$.

78. Expand $2(x + 5) + 3(x - 2)$.

79. Simplify $7a - 3(b - a)$ (expand first).

80. Expand $(x + 5)^2$.

81. Simplify $4(2x + 1) - 3(x - 2)$.

82. Expand $3x(2x + 1)$.

83. Simplify $5x - 2(3 - x)$.

84. Expand $(2x - 1)(3x + 2)$.

85. Simplify $x + x + y + y + y$.

86. Expand $6( x - \dfrac{1}{2})$.

87. Simplify $9 - 4x + 2x - 1$.

88. Expand $2(3a - 2b) + 4(a + b)$.

89. Simplify $3(x + 2) - 2(x + 5)$.

90. Expand $(x + 1)(x - 1)$.

91. Simplify $4x^2 + 2x + x^2 - 3x$.

92. Expand $5(2x - 3y) - 2(x + y)$.

## More reasoning (AO2)

93. **True or false?** $2(x + 3)$ and $2x + 3$ are equivalent. Test with $x = 4$.

94. **Always, sometimes, or never?** Expanding brackets then collecting like terms gives a unique simplified form.

95. Leo expands $3(x + 2)$ as $3x + 2$. Correct him with the right expansion.

96. **True or false?** $x + x^2$ can be simplified to $2x^2$. Explain.

97. **Always, sometimes, or never?** $-(x - 3) = -x + 3$.

98. Nina says $5x + 2x = 7x^2$. Explain the error.

99. **True or false?** $(x + 2)^2 = x^2 + 4$. Test with $x = 1$.

100. **Always, sometimes, or never?** $2(3x) = 6x$.

101. **True or false?** $3x + 2y$ and $2y + 3x$ are the same expression.

102. **Always, sometimes, or never?** $x^2 + x^2 = 2x^2$.

103. Omar expands $(x + 3)(x + 2)$ as $x^2 + 5x + 5$. Find the mistake.

104. **True or false?** $4(x - 1) - 4x = -4$ for every $x$.

105. **Always, sometimes, or never?** $a(b + c) = ab + ac$.

106. **True or false?** $5x - 3x = 2$ when $x = 2$. Evaluate both sides.

107. **Always, sometimes, or never?** $(x + 1)^2 > x^2 + 1$ for $x > 0$.

108. Priya writes $2(x + y) = 2x + y$. Show a counter-example with $x = 1$, $y = 1$.

109. **True or false?** $3(2x - 1) = 6x - 1$.

110. **Always, sometimes, or never?** $x + 2x = 3x$.

111. **True or false?** $-(2x - 5) = -2x - 5$.

112. **Always, sometimes, or never?** $(x + a)(x + b) = x^2 + (a + b)x + ab$.

113. **True or false?** $5( x + 0) = 5x$.

114. **Always, sometimes, or never?** $2x + 3y$ has exactly two terms.

115. **True or false?** $x^2 + 2x + 1 = (x + 1)^2$ is an identity.

116. **Always, sometimes, or never?** $4x - 2x = 2x$.

117. **True or false?** $3(x - 2) - 3x = -6$.

118. **Always, sometimes, or never?** Expanding $-1(x - 4)$ gives $-x + 4$.

## Problem practice (AO3)

119. A rectangle has sides $x$ and $x + 5$. Write and simplify an expression for its perimeter.

120. A shop sells $n$ notebooks at £2 each and 3 pens at £1 each. Write total cost.

121. A garden bed is $2(x + 3)$ metres long and $x$ metres wide. Expand length.

122. A triangle has sides $x$, $2x$, and $x + 4$. Simplify the perimeter expression.

123. Archer buys $x$ sandwiches at £3 and $x$ drinks at £1.50. Write and simplify total spend.

124. A wire is $(3x + 7)$ cm long; $2x$ cm is cut off. Write remaining length.

125. A poster has area $x(x + 2)$. Expand the area expression.

126. A taxi fare is £3 plus £2 per mile for $m$ miles: expand $3 + 2m$ (already expanded — write cost for 5 miles using expression).

127. A class has $2x$ boys and $x + 3$ girls. Write and simplify total pupils.

128. A phone case costs £$(2x + 5)$; buy 3 cases. Expand total.

129. A path is $4(x - 1)$ metres. Expand, then find length when $x = 6$.

130. A sum of two consecutive even numbers is $2x + 2$. Expand if written as $x + (x + 2)$.

131. A pool tile border adds 2 tiles to each side of an $x$ by $x$ square. Write perimeter with tiles if each side becomes $x + 2$.

132. Simplify the total of $5( x + 2) + 3( x - 1) + x$.

133. A volume model is $x \cdot x \cdot (x + 1)$. Expand as far as possible for $x^2(x + 1)$.

134. A discount takes £2 off each of $n$ items priced £$(x + 5)$. Write total before discount.

135. A shape perimeter is $2(3x + 1) + 2(x + 4)$. Expand and simplify.

136. A sum of three consecutive integers is $3x + 3$. Show this by writing $x + (x + 1) + (x + 2)$.

137. A cuboid has dimensions $x$, $x + 1$, and $2$. Write volume expression and expand.

138. A pattern adds 4 tiles per row: row length $x + 4$. Expand $3(x + 4)$ for three rows.

139. A simplified expression for $4(2x - 3) - 2( x + 5)$ — find it.

140. A square of side $2x$ has perimeter $8x$. Show by expanding $4(2x)$.

141. A sum $x + 3(x - 2) + 5$ — simplify.

142. A rectangle area $3x(x + 2)$ — expand.

143. A expression $2(x + 1)^2 - 2x^2$ — expand and simplify.

144. A fence has sections $x + 2$ and $3x - 1$. Write total length.

145. A shop margin: selling price $1.2( x + 10)$ — expand.

146. A sum of squares of two consecutive numbers: $x^2 + (x + 1)^2$ — expand.

147. A simplify $5x - 2(2x - 3) + 4$.

148. A expand $(x + 4)(x - 2)$.

149. A pool chlorination $0.5(2x + 6)$ — expand.

150. A total $3(2x + 1) + 2(3x - 4)$ — simplify.

151. A expression for double $(x + 5)$ minus $(x - 1)$ — simplify.

152. A expand $4x( x - 3)$.

153. A simplify $7(x - 2) - 3(2x + 1)$.

154. A perimeter $2(x + 3) + 2(2x - 1)$ — simplify.

155. A expand $(2x + 3)(x + 1)$.

156. A simplify $x( x + 5) - x^2$.

157. A expand $5( x + y + 2)$.

158. A simplify $6x - 3( x - 4) + 2$.

159. A expand $(x + 6)(x + 1)$.

160. A simplify $4(3x - 2) - 5( x + 1)$.

161. A expand $(3x - 2)(2x + 5)$.

162. A simplify $2(x + 3)^2 - 2x^2 - 12x$ (expand squares first).

163. A expand $x(2x - 3)(x + 1)$ — multiply two brackets first.

164. A simplify $\dfrac{12x + 8}{4} + 3x$.

## Stretch challenges

165. Expand and simplify $(2x - 1)(3x + 4) - (x + 2)^2$.

166. **True or false?** $(x + y)^2 = x^2 + y^2 + 2xy$. Expand LHS for $x = 1$, $y = 2$.

167. Simplify $3(2x - 1)^2 - 12x$ (expand step by step).

168. Expand $(x + 2)(x + 3)(x + 1)$.

169. **Always, sometimes, or never?** $(x + a)^2 \geq 0$ for real $x$ when $a$ is real.

170. Simplify $5x - 2(3x - 4) + (x - 5)^2$ for $x = 0$ only (substitute after simplify attempt).

171. Expand $(2x + 1)(2x - 1)$.

172. **True or false?** $x^3 + x^3 = 2x^3$.

173. Simplify $4( x + 1)(x - 1) - 2(x^2 - 4)$.

174. Expand $(x + 5)^2 - (x - 5)^2$.

175. **Always, sometimes, or never?** $2x^2 = (2x)^2$.

176. Simplify $\dfrac{3(2x - 6)}{6} + x$.

177. Expand $(3x - 2y)(x + 4y)$.

178. **True or false?** $(-x)^2 = x^2$.

179. Simplify $2(3x + 1) - 3(2x - 2) + (x + 4)$.

180. Expand $(x + a)(x + b)$ with $a = 2$, $b = 5$ and compare to $x^2 + 7x + 10$.

181. **Always, sometimes, or never?** $x^2 - 1$ factors as $(x - 1)(x + 1)$.

182. Simplify $5(2x - 3)^2 - 20x + 45$ (spot a square).

183. Expand $(2x + 3)^2 - (x - 1)^2$.

184. **True or false?** $3x + 6 = 3(x + 2)$ is an identity.

185. Simplify $x( x + 1)(x + 2) - x^3 - 3x^2$.

186. Expand $(x + \dfrac{1}{2})^2$.

187. **Always, sometimes, or never?** $(x + y)(x - y) = x^2 - y^2$.

188. Simplify $4x^2 - (2x - 1)^2$.

189. Expand $(x + 2)^3$ as far as GCSE expansion (multiply stepwise).

190. **True or false?** $10x - 5x = 5x$.

191. Simplify $\dfrac{2(3x + 9)}{3} - 2x$.

192. Expand $(5x - 1)(5x + 1)$.

193. **Always, sometimes, or never?** $x^2 + 2x + 1$ is a perfect square.

194. Simplify $3( x + 2)(x - 2) + 4x^2$.

195. Expand $(2x - 3y)^2$.

196. **True or false?** $-(x - y) = y - x$.

197. Simplify $2x(3x - 4) - 3(2x^2 - 5x)$.

198. Expand $(x + 1)(x + 2)(x + 3)$ — multiply two brackets first.

199. **Always, sometimes, or never?** $a^2 - b^2 = (a - b)(a + b)$.

200. Create your own expression with three brackets to expand, then write the fully simplified result (show working).

---

*GCSE link: Foundation expanding and simplifying (A4) supports solving equations and graphs.*
