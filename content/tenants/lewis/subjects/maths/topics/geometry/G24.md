---
title: Translation vectors
topic: G24
domain: geometry
ao:
  - AO1
  - AO2
  - AO3
printNotes: "Name · Date · A column vector $\binom{x}{y}$ means move $x$ right (or left if negative) and $y$ up (or down if negative)"
---

## Getting started

1. Start on a square and move 2 squares right. How many horizontal squares did you move?
2. Start on a square and move 3 squares up. How many vertical squares did you move?
3. Which direction is opposite to right?
4. Which direction is opposite to up?
5. Start at (0, 0) and move 2 right. Where do you finish?
6. Start at (0, 0) and move 3 up. Where do you finish?
7. Start at (1, 1) and move 2 right. Where do you finish?
8. A shape moves 4 left and does not move up or down. What is its vertical movement?

## Fluency (AO1)

9. Write the column vector that describes a movement of 3 right and 2 up.
10. Write the column vector that describes a movement of 4 left and 1 up.
11. Write the column vector that describes a movement of 5 right and 3 down.
12. Point A is at $(2, 3)$. It is translated by the vector $\binom{4}{1}$. Find the coordinates of the image point.
13. Point B is at $(0, 0)$. It is translated by the vector $\binom{-3}{5}$. Find the coordinates of the image point.
14. Point C is at $(6, -2)$. It is translated by the vector $\binom{-2}{-4}$. Find the coordinates of the image point.
15. Point D is at $(-1, -1)$. It is translated by the vector $\binom{5}{2}$. Find the coordinates of the image point.
16. Point A is at $(1, 2)$ and its image after translation is $(5, 6)$. Find the column vector of the translation.
17. Point B is at $(3, 7)$ and its image after translation is $(3, 2)$. Find the column vector of the translation.
18. Point C is at $(-4, 2)$ and its image after translation is $(0, 0)$. Find the column vector of the translation.
19. What single word describes a transformation that slides a shape without turning or flipping it?
20. If a shape is translated by $\binom{0}{0}$, what happens to the shape?


## More fluency

21. Write the vector for a translation 3 right and 2 up.

22. Write the vector for 4 left and 1 down.

## Reasoning (AO2)

23. Mia says that translating a shape by $\binom{2}{3}$ makes it move 2 up and 3 right. Explain her mistake.

24. **True or false?** Translation always produces an image that is congruent to the original shape.

25. Ravi translates point $(4, 5)$ by $\binom{-6}{2}$ and gets $(2, 7)$. Check his working and correct it if needed.

26. **Always, sometimes, or never?** Translating a shape changes its size or orientation.

27. Priya says the vector $\binom{3}{-3}$ describes the same movement as $\binom{-3}{3}$. Explain why she is wrong.

28. Explain how you would find the column vector describing the translation from point $A(2, 1)$ to point $B(7, 4)$.

29. Sloan translates a shape by $\binom{5}{0}$. Describe what happens to the shape's position, and explain why the $y$-coordinates of every point stay the same.

30. Leo says that translating a shape twice by the same vector $\binom{2}{1}$ is the same as translating it once by $\binom{4}{2}$. Is he correct? Explain.

## Problem (AO3)

31. Triangle A has vertices $(1, 1)$, $(3, 1)$, and $(1, 4)$. It is translated by the vector $\binom{2}{-3}$. Find the coordinates of the translated triangle's vertices.

32. A shape is translated by $\binom{4}{2}$ and then by $\binom{-1}{3}$. Find the single column vector that describes the overall (combined) translation.

33. A point starts at $(5, 5)$, is translated by $\binom{-2}{4}$, and then translated again by $\binom{3}{-6}$. Find the final coordinates of the point.

34. A robot on a grid starts at $(0, 0)$. It follows three translations in order: $\binom{3}{2}$, $\binom{-1}{4}$, $\binom{2}{-5}$. Find the robot's final position.

35. A quadrilateral has vertices $(0, 0)$, $(2, 0)$, $(2, 3)$, and $(0, 3)$. Find the coordinates after translation by $\binom{-3}{-1}$, then describe whether the shape's size and angles have changed.

36. The vector from point $P$ to point $Q$ is $\binom{6}{-2}$. If $P$ is at $(-1, 5)$, find the coordinates of $Q$.

## Stretch

37. Point $A(3, -2)$ is translated to point $B(-1, 6)$. Find the column vector of this translation, then find the vector that would translate $B$ back to $A$.

38. A shape is translated by vector $\binom{a}{b}$ so that the point $(2, -3)$ maps to $(6, 1)$. Find the values of $a$ and $b$.

39. A shape undergoes two translations, $\binom{x}{2}$ then $\binom{3}{y}$, and the overall effect is the same as a single translation of $\binom{7}{-1}$. Find the values of $x$ and $y$.

40. Translate $(2,-1)$ by $\binom{-5}{4}$. Give the image point and the vector that returns it to $(2,-1)$.


## Mixed practice

41. Point (2, 5) is translated by $\begin{pmatrix} 3 \\ -1 \end{pmatrix}$. Find the image.

42. A shape moves 5 units left and 3 units up. Write the translation vector.

## More practice (continued)

43. Describe the translation that moves the origin to (-5, -3).

44. Describe the translation that moves the origin to (-5, -2).

45. Point (1, 2) is translated by $\binom{2}{3}$. Find the image coordinates.

46. Point (3, 4) is translated by $\binom{-1}{4}$. Find the image coordinates.

47. Point (0, 0) is translated by $\binom{5}{-2}$. Find the image coordinates.

48. Point (-2, 5) is translated by $\binom{-3}{-3}$. Find the image coordinates.

49. Point (6, -1) is translated by $\binom{0}{6}$. Find the image coordinates.

50. Point (-4, -3) is translated by $\binom{4}{0}$. Find the image coordinates.

51. Point (1, 2) is translated by $\binom{2}{3}$. Find the image coordinates.

52. Point (3, 4) is translated by $\binom{-1}{4}$. Find the image coordinates.

53. Point (0, 0) is translated by $\binom{5}{-2}$. Find the image coordinates.

54. Point (-2, 5) is translated by $\binom{-3}{-3}$. Find the image coordinates.

55. Point (6, -1) is translated by $\binom{0}{6}$. Find the image coordinates.

56. Point (-4, -3) is translated by $\binom{4}{0}$. Find the image coordinates.

57. Point (1, 2) is translated by $\binom{2}{3}$. Find the image coordinates.

58. Point (3, 4) is translated by $\binom{-1}{4}$. Find the image coordinates.

59. Point (0, 0) is translated by $\binom{5}{-2}$. Find the image coordinates.

60. Point (-2, 5) is translated by $\binom{-3}{-3}$. Find the image coordinates.

61. Point (6, -1) is translated by $\binom{0}{6}$. Find the image coordinates.

62. Point (-4, -3) is translated by $\binom{4}{0}$. Find the image coordinates.

63. Point (1, 2) is translated by $\binom{2}{3}$. Find the image coordinates.

64. Point (3, 4) is translated by $\binom{-1}{4}$. Find the image coordinates.

65. Point (0, 0) is translated by $\binom{5}{-2}$. Find the image coordinates.

66. Point (-2, 5) is translated by $\binom{-3}{-3}$. Find the image coordinates.

67. Point (6, -1) is translated by $\binom{0}{6}$. Find the image coordinates.

68. Point (-4, -3) is translated by $\binom{4}{0}$. Find the image coordinates.

69. Point A is (1, 2) and its image B is (4, 4). Find the translation vector.

70. Point A is (3, 4) and its image B is (7, 7). Find the translation vector.

71. Point A is (0, 0) and its image B is (5, 4). Find the translation vector.

72. Point A is (-2, 5) and its image B is (4, 7). Find the translation vector.

73. Point A is (6, -1) and its image B is (9, 2). Find the translation vector.

74. Point A is (-4, -3) and its image B is (0, 1). Find the translation vector.

75. Point A is (1, 2) and its image B is (6, 4). Find the translation vector.

76. Point A is (3, 4) and its image B is (9, 7). Find the translation vector.

77. Point A is (0, 0) and its image B is (3, 4). Find the translation vector.

78. Point A is (-2, 5) and its image B is (2, 7). Find the translation vector.

79. Point A is (6, -1) and its image B is (11, 2). Find the translation vector.

80. Point A is (-4, -3) and its image B is (2, 1). Find the translation vector.

81. Point A is (1, 2) and its image B is (4, 4). Find the translation vector.

82. Point A is (3, 4) and its image B is (7, 7). Find the translation vector.

83. Point A is (0, 0) and its image B is (5, 4). Find the translation vector.

84. Point A is (-2, 5) and its image B is (4, 7). Find the translation vector.

85. Point A is (6, -1) and its image B is (9, 2). Find the translation vector.

86. Point A is (-4, -3) and its image B is (0, 1). Find the translation vector.

87. Point A is (1, 2) and its image B is (6, 4). Find the translation vector.

88. Point A is (3, 4) and its image B is (9, 7). Find the translation vector.

89. A shape is translated by $\binom{2}{-1}$ then by $\binom{-1}{3}$. Find the single equivalent vector.

90. A shape is translated by $\binom{3}{0}$ then by $\binom{-1}{4}$. Find the single equivalent vector.

91. A shape is translated by $\binom{4}{-1}$ then by $\binom{-1}{3}$. Find the single equivalent vector.

92. A shape is translated by $\binom{2}{0}$ then by $\binom{-1}{4}$. Find the single equivalent vector.

93. A shape is translated by $\binom{3}{-1}$ then by $\binom{-1}{3}$. Find the single equivalent vector.

94. A shape is translated by $\binom{4}{0}$ then by $\binom{-1}{4}$. Find the single equivalent vector.

95. A shape is translated by $\binom{2}{-1}$ then by $\binom{-1}{3}$. Find the single equivalent vector.

96. A shape is translated by $\binom{3}{0}$ then by $\binom{-1}{4}$. Find the single equivalent vector.

97. A shape is translated by $\binom{4}{-1}$ then by $\binom{-1}{3}$. Find the single equivalent vector.

98. A shape is translated by $\binom{2}{0}$ then by $\binom{-1}{4}$. Find the single equivalent vector.

99. A shape is translated by $\binom{3}{-1}$ then by $\binom{-1}{3}$. Find the single equivalent vector.

100. A shape is translated by $\binom{4}{0}$ then by $\binom{-1}{4}$. Find the single equivalent vector.

101. A shape is translated by $\binom{2}{-1}$ then by $\binom{-1}{3}$. Find the single equivalent vector.

102. A shape is translated by $\binom{3}{0}$ then by $\binom{-1}{4}$. Find the single equivalent vector.

103. A shape is translated by $\binom{4}{-1}$ then by $\binom{-1}{3}$. Find the single equivalent vector.

104. A shape is translated by $\binom{2}{0}$ then by $\binom{-1}{4}$. Find the single equivalent vector.

105. Write the vector for 6 right and 2 down.

106. Write the vector for 3 left and 4 up.

107. Write the vector for 5 up.

108. Write the vector for 7 left.

109. **True or false?** Translation changes the size of a shape.

110. **Always, sometimes, or never?** Translation vectors add top number first (horizontal).

111. Explain how to undo a translation.

112. Describe the translation that maps the origin to (-4, 4).

113. Describe the translation that maps the origin to (-3, -3).

114. Describe the translation that maps the origin to (-2, -1).

115. Describe the translation that maps the origin to (-1, 1).

116. Describe the translation that maps the origin to (0, 3).

117. Describe the translation that maps the origin to (1, -4).

118. Describe the translation that maps the origin to (2, -2).

119. Describe the translation that maps the origin to (3, 0).

120. Describe the translation that maps the origin to (4, 2).

121. Describe the translation that maps the origin to (5, 4).

122. Describe the translation that maps the origin to (-5, -3).

123. Describe the translation that maps the origin to (-4, -1).

124. Describe the translation that maps the origin to (-3, 1).

125. Describe the translation that maps the origin to (-2, 3).

126. Describe the translation that maps the origin to (-1, -4).

127. Describe the translation that maps the origin to (0, -2).

128. Describe the translation that maps the origin to (1, 0).

129. Describe the translation that maps the origin to (2, 2).

130. Describe the translation that maps the origin to (3, 4).

131. Describe the translation that maps the origin to (4, -3).

132. Describe the translation that maps the origin to (5, -1).

133. Describe the translation that maps the origin to (-5, 1).

134. Describe the translation that maps the origin to (-4, 3).

135. Describe the translation that maps the origin to (-3, -4).

136. Describe the translation that maps the origin to (-2, -2).

137. Describe the translation that maps the origin to (-1, 0).

138. Describe the translation that maps the origin to (0, 2).

139. Describe the translation that maps the origin to (1, 4).

140. Describe the translation that maps the origin to (2, -3).

141. Describe the translation that maps the origin to (3, -1).

142. Describe the translation that maps the origin to (4, 1).

143. Describe the translation that maps the origin to (5, 3).

144. Describe the translation that maps the origin to (-5, -4).

145. Describe the translation that maps the origin to (-4, -2).

146. Describe the translation that maps the origin to (-3, 0).

147. Describe the translation that maps the origin to (-2, 2).

148. Describe the translation that maps the origin to (-1, 4).

149. Describe the translation that maps the origin to (0, -3).

150. Describe the translation that maps the origin to (1, -1).

151. Describe the translation that maps the origin to (2, 1).

152. Describe the translation that maps the origin to (3, 3).

153. Describe the translation that maps the origin to (4, -4).

154. Describe the translation that maps the origin to (5, -2).

155. Describe the translation that maps the origin to (-5, 0).

156. Describe the translation that maps the origin to (-4, 2).

157. Describe the translation that maps the origin to (-3, 4).

158. Describe the translation that maps the origin to (-2, -3).

159. Describe the translation that maps the origin to (-1, -1).

160. Describe the translation that maps the origin to (0, 1).

161. Describe the translation that maps the origin to (1, 3).

162. Describe the translation that maps the origin to (2, -4).

163. Describe the translation that maps the origin to (3, -2).

164. Describe the translation that maps the origin to (4, 0).

165. Describe the translation that maps the origin to (5, 2).

166. Describe the translation that maps the origin to (-5, 4).

167. Describe the translation that maps the origin to (-4, -3).

168. Describe the translation that maps the origin to (-3, -1).

169. Describe the translation that maps the origin to (-2, 1).

170. Describe the translation that maps the origin to (-1, 3).

171. Describe the translation that maps the origin to (0, -4).

172. Describe the translation that maps the origin to (1, -2).

173. Describe the translation that maps the origin to (2, 0).

174. Describe the translation that maps the origin to (3, 2).

175. Describe the translation that maps the origin to (4, 4).

176. Describe the translation that maps the origin to (5, -3).

177. Describe the translation that maps the origin to (-5, -1).

178. Describe the translation that maps the origin to (-4, 1).

179. Describe the translation that maps the origin to (-3, 3).

180. Describe the translation that maps the origin to (-2, -4).

181. Describe the translation that maps the origin to (-1, -2).

182. Describe the translation that maps the origin to (0, 0).

183. Describe the translation that maps the origin to (1, 2).

184. Describe the translation that maps the origin to (2, 4).

185. Describe the translation that maps the origin to (3, -3).

186. Describe the translation that maps the origin to (4, -1).

187. Describe the translation that maps the origin to (5, 1).

188. Describe the translation that maps the origin to (-5, 3).

189. Describe the translation that maps the origin to (-4, -4).

190. Describe the translation that maps the origin to (-3, -2).

191. Describe the translation that maps the origin to (-2, 0).

192. Describe the translation that maps the origin to (-1, 2).

193. Describe the translation that maps the origin to (0, 4).

194. Describe the translation that maps the origin to (1, -3).

195. Describe the translation that maps the origin to (2, -1).

196. Describe the translation that maps the origin to (3, 1).

197. Describe the translation that maps the origin to (4, 3).

198. Describe the translation that maps the origin to (5, -4).

199. Describe the translation that maps the origin to (-5, -2).

200. Describe the translation that maps the origin to (-4, 0).


---

*GCSE link: translations on a coordinate grid using column vectors (Foundation G24).*
