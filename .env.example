# Vercel KV (create store in Vercel → Storage → KV, link to this project)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: Lichess opening explorer (chess trainer). Without it, legal-move fallback is used.
LICHESS_EXPLORER_TOKEN=
