# Interactive bar-chart answers (deferred)

**Status:** backlog — not in scope until read-only question figures are solid

Practice today uses **numeric height inputs** for `kind: "bar-chart"` (three items: lewis **S1** Q27, Q126, Q130). Stems say “draw a bar chart”; that mismatch is acceptable for now.

## Later (when prioritised)

- Visual **bar builder** (stepped or drag), shared look with read-only `FigureView` bar charts.
- Client meta must **not** ship `bars.heights` (grade on server only).
- Composite grading for Q126/Q130 “mode / most popular” text vs heights-only today.

## Do not block

Content and figure waves for **showing** data in questions (`{CODE}.figures.json`). See `FIGURES-BRIEF.md` and `issues/007-read-only-figures-quality.md`.
